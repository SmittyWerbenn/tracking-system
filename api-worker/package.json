{
  "name": "gms-api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply gms-db --local",
    "migrate:remote": "wrangler d1 migrations apply gms-db --remote"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "typescript": "~5.7.2",
    "wrangler": "^4.131.1"
  }
}
